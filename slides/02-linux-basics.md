```markdown
# Linux Basics — Slides

## Core topics
- What is Linux and where it runs
- Linux architecture: kernel, userspace, distributions
- Filesystem layout: /, /etc, /var, /home, /usr

## Key commands to demo
- `pwd`, `ls`, `cd`
- `cat`, `less`, `head`, `tail`
- `mkdir`, `rm`, `cp`, `mv`

## Presenter prompts
- Show `ls -la` and explain hidden files.
- Demonstrate creating a small file and viewing it with `less`.
- Map directories to real-world examples (logs, configs, user files).

```
