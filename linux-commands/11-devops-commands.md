# DevOps-Focused Commands

- `systemctl start/stop/status service` → manage services
- `journalctl -xe` → view system logs
- `crontab -e` → schedule tasks
- `docker ps` → list containers (if Docker installed)
- `kubectl get pods` → check Kubernetes pods
